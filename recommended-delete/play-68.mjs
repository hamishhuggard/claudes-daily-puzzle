import content from"../tools/content/068.js";import{countSolutions,check,par}from"../engines/shakashaka-rules.js";let f=0;for(const[r,i]of content.data.rounds.map((x,i)=>[x,i])){const s=countSolutions(r.spec,2),g=check(r.spec,r.answer);console.log(`round ${i+1}: solutions=${s.count} exhausted=${s.exhausted} par=${par(r.spec)} grade=${g.ok}`);if(s.count!==1||!s.exhausted||!g.ok)f++;}if(f)process.exit(1);console.log("All #68 checks passed.");
